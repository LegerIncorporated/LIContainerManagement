import XCTest
@testable import LIContainerManagement

final class LIContainerManagementTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(LIContainerManagement().text, "Hello, World!")
    }
}
